import { outputSpecialAttacks } from "../app";

test("testing the output of special Attacks with correct input data", () => {
  const obj = {
    name: 'Лучник',
    type: 'Bowman',
    health: 50,
    level: 3,
    attack: 40,
    defence: 10,
    special: [
      {
        id: 8,
        name: 'Двойной выстрел',
        icon: 'http://...',
        description: 'Двойной выстрел наносит двойной урон'
      }, 
      {
        id: 9,
        name: 'Нокаутирующий удар',
        icon: 'http://...'
      }
    ]	
  };

  const expectedResult = [
    {
      id: 8,
      name: 'Двойной выстрел',
      icon: 'http://...',
      description: 'Двойной выстрел наносит двойной урон'
    }, 
    {
      id: 9,
      name: 'Нокаутирующий удар',
      icon: 'http://...',
      description: 'Описание недоступно'
    }, 
  ];

  const result = outputSpecialAttacks(obj);
  expect(result).toEqual(expectedResult);
});

test("testing the output of special Attacks with an an empty special array", () => {
  const obj = {
    name: 'Лучник',
    type: 'Bowman',
    health: 50,
    level: 3,
    attack: 40,
    defence: 10,
    special: []
  }
  const expectedResult = [];
  const result = outputSpecialAttacks(obj);
  expect(result).toEqual(expectedResult);
});